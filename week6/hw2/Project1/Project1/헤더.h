#pragma once

//파라미터가 있는 최대공약수 구하는 함수
int max1(int a, int b);
//파라미터가 없는 최대공약수 구하는 함수
void max2(void);

//파라미터가 있는 최소공배수 구하는 함수
int min1(int a, int b);
//파라미터가 없는 최소공배수 구하는 함수
void min2(void);

//파라미터가 있는 소수 판별함수
int prime1(int num);
//파라미터가 없는 소수 판별함수
void prime2(void);